apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodeapp-k8s-project.fullname" . }}-test-connection"
  labels:
    {{- include "nodeapp-k8s-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodeapp-k8s-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
